apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: apache-webserver
spec:
  replicas: 4
  template:
    metadata:
      labels:
        app: apache
    spec:
      containers:
        - name: apache
          image: httpd
          ports:
            - containerPort: 80